                            NeuNet

程序功能简介
    neunet 是用来根据全球分布的IGS跟踪站，求解卫星小数偏差的程序。在利用neunet程序
进行求解时，首先需要准备neunet的配置文件neunet.conf，该文件包括了进行数据处理时的一些
处理策略的参数，也包括了一些输入文件，比如PRN_GPS, OCEAN.dat, clock/orbit 文件以及
观测数据RINEX文件。
    根据目前研究表明，不同类型接收机对应的卫星小数偏差不同，目前IGS跟踪站的接收机类型
主要分为三类：（1）Y-codeless接收机，该类型接收机的观测数据为p1,p2; (2) 一些non-codeless类型
接收机，该类型接收机发布的观测类型为c1,p2,但是只需要将c1观测值即可; (3) cross-correlation类型
接收机，但是该类型接收机的c1,p2均需要修改为p1,p2观测值。
    目前IGS推荐是将后两个类型的接收机采用cc2noncc的程序将对应的观测值校正了IGS发布的小数偏差
后，即可实现三类接收机的统一。然而由于过去确定的不同类型码观测值之间的系统偏差是采用了伪距观测值，
且在估计小数偏差时，认为其在一个月是稳定的。而ppp非差模糊度固定的研究表明，窄巷小数偏差的变化在15min尺度，因此
如果利用全球跟踪站网路确定卫星小数偏差时，直接采用目前IGS发布的p1c1bia产品，将难以保证不同类型
观测值能够项目匹配。
    因此，本项目研究中，将三类不同的观测站分类，然后在改正了IGS发布的小数偏差后，再分别
利用不同的台站，确定该类型接收机的GPS卫星upd参数，通过比较给出不同接收机的upd参数的差别。
如果不同类型接收机的upd差别影响到了模糊度固定，则应该发布新的小数偏差，以提高不同类型接收机
的模糊度固定。
    
    针对不同类型的接收机，配置文件的准备顺序为：
     (1) 调用download_ephemeris.sh和download_rinex.sh观测文件，下载所需要的观测数据，精密星历等数据；
    （2）调用get_station_list_p1p2.sh, 根据p1c1bias.hist, 生成yc类型接收机的测站列表；
         另外，调用get_station_list_c1only.sh和get_station_list_c1p2.sh分别生成另外2类观测台站列表；
     (3) 利用gmt_station_p1p2.sh和gmt_station_c1only.sh画图，然后选择用来确定
         卫星小数偏差的参考站列表reference.p1p2.list和reference.c1only.list,并
         绘图，保证参考站数量在100个左右，且全球分布均匀。
     (4) 根据参考站列表，确定OCEAN-GOT00.dat全球跟踪台站的海洋潮汐文件。
         利用Hans-Georg Scherneck网站（http://holt.oso.chalmers.se/loading/）上提供的信息确定
         每个测站的潮汐系数。
     (5) 根据参考站列表(p1p2和c1only两类），利用teqc工具合并需要处理的观测文件，为后面数据处理做准备
         根据teqc合并文件后输出的信息，删除数据空白比较多的测站，以及出现其他异常的测站，比如zeck
         就提示receiver firmware change的警告。
    （6）利用ppp程序，采用动态模式，对所有测站数据进行精密单点定位处理，根据定位
的残差值，剔除测数据不好的观测台站，然后根据全球测站的分布选择60个左右的台站，作为
参考站；
    （7）调用gen_conf.sh, 根据上面选择的参考站列表和IGS发布的测站的周解坐标ssc文件，确定配置文件；
